# Calculadora Primitiva, esta toma dos valores reales, un simbolo, y los opera.
def calculadora(a,b,x):
    if(x == "+"):
        return (a+b)
    if(x == "-"):
        return (a-b)
    if(x == "*"):
        return (a*b)
    if(x == "/"):
        return (a/b)
